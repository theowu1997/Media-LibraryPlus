Place Windows FFmpeg binaries here for packaged builds.

Expected example layout:

- resources/ffmpeg/win32-x64/ffmpeg.exe
- resources/ffmpeg/win32-x64/ffprobe.exe

Electron Builder copies this folder into the packaged app as an extra resource.
